# int(): 정수 -1,2,3,-1,0
# float(): 부동소수점 -1.0, 2.0 3.2, -1.0
ex)a = int(input("첫번째 숫 자를 입력해주세요")) 이러면 문자열이 숫자로 변환된다
#str(): string 문자열의 약자 이 함수를 사용해서 숫자를 문자열로 변환한다.

a = int(input("첫번째 숫 자를 입력해주세요")) 이걸
a = input("첫번째 숫자를 입력해주세요")
a = float(a)
이렇게 나눠줄수도 있다.